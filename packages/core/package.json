{
  "name": "core",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rescript build",
    "clean": "rescript clean",
    "test": "vitest run"
  },
  "dependencies": {
    "@rescript/runtime": "^12.0.0"
  },
  "devDependencies": {
    "rescript": "^12.0.0",
    "vitest": "^3.2.0"
  }
}
